export interface IQueryParams {
  pageIndex?: string;
  pageSize?: string;
  sortBy?: string;
  searchValue?: string;
  sortDirection?: string;
}

export interface IMessageResponse {
	message: string;
}